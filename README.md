# MPP Football GitHub Pages site

This repository contains a GitHub Actions workflow and scripts that build a small static site (served from the gh-pages branch) to display an MPP league scoreboard and the World Cup 2026 schedule.

The workflow (on main) fetches the World Cup schedule and the MPP league page every hour and updates the gh-pages branch with the latest data.
